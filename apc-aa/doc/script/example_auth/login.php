<?php   require_once __DIR__."/head.php";   // checkAuth() must be called before any output of the page   // (since it sets cookies)   $is_valid_user = $a->checkAuth();?><h1>Profile page</h2><p>There is some content.</p><br><?phpif ($is_valid_user) {    echo "Login sucessfull!!!<br/> You are ". $a->getUid();    // and here we probably display some user's data} else {    echo "Login incorrect";    // and we can display the userbox here as well   displayUserbox($a); // the $a variable is defined in head.php - see above}?><p>That's it - the box is displayed and the content continues.   <br>go to other <a href="test.php">test page</a></p>